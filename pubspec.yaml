
name: library_frontend
description: Aplikasi Peminjaman Buku
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  provider: ^6.1.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  mockito: ^5.4.4
  build_runner: ^2.4.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true